---
id: ST-07
type: story-active
status: in-sprint
sprint: SPRINT-001
sp-original: 5
sp-effective: 5
created: 2026-06-27
---

# 🏃 [Active] ST-07 — Extract PDF Text

> **Thin status wrapper** cho Sprint 001. **AC & traceability đầy đủ xem story canonical — không lặp lại tại đây:**
> 📄 [`../Backlog/Story-Extract-PDF-Text.md`](../Backlog/Story-Extract-PDF-Text.md)
>
> Sprint Plan: [`../../010-Planning/Sprints/Sprint-001.md`](../../010-Planning/Sprints/Sprint-001.md) · SP gốc **5** · SP hiệu dụng **5** (full work — `PdfAdapter.extract` stub).

## Đóng góp cho Sprint Goal

Là **lõi "Trích xuất"** đóng vòng vertical slice: biến PDF đang xem thành dữ liệu text khai thác được (sẵn sàng cho search/AI ở các milestone sau). Hoàn thành ST-07 nghĩa là pipeline "Mở → Xem → Trích xuất" đã xanh đầu-cuối với PDF.

## Task Breakdown (Engineer thực hiện)

- [ ] Hiện thực `extract(file)` trong `src/core/adapters/PdfAdapter.ts`: dùng PDF.js `page.getTextContent()` → `ExtractedContent` (`textBlocks` theo thứ tự trang & thứ tự đọc cơ bản).
- [ ] Chạy extract off-main-thread qua `src/core/worker.ts` (WorkerRequest `extract` cho PDF), giữ Core thuần (KR3.1).
- [ ] Wire `DocumentService.open()` gọi `adapter.extract` + `getExtracted(sessionId)`; dispatch `EXTRACTION_READY`.
- [ ] Hiện thực `src/ui/ExtractedContentPanel.tsx`: hiển thị nội dung trích xuất + trạng thái (`Ready`/`Empty`/`Failed`) thay placeholder.
- [ ] Xử lý unhappy: PDF scan/không text layer → `status: Empty`, báo "không có text trích xuất được", **vẫn view được** (Unhappy Path 1); nội dung hỏng → `Failed`, không tạo kết quả (Unhappy Path 2).

## Definition of Done (story-level — map từ AC canonical)

- [ ] PDF có text layer → text bóc tách & hiển thị ở panel; sẵn sàng cho search/feed AI.
- [ ] Text trả về theo thứ tự trang + thứ tự đọc cơ bản.
- [ ] Logic extract chạy độc lập UI/user layer (KR3.1).
- [ ] PDF scan → "không có text trích xuất được", vẫn view bình thường (Unhappy Path 1).
- [ ] Nội dung hỏng → báo lỗi, không tạo kết quả trích xuất (Unhappy Path 2).

> OCR cho PDF scan: **Out of scope** (defer M2).

## Dependencies

- **Phụ thuộc:** ST-01 (session đã `open`), ST-03 (PDF đã mở/render), ST-06 (`ExtractedContentPanel` trong khung Unified Viewer).
- **Được phụ thuộc bởi:** (ngoài Sprint 1) ST-11 Search — cần `ExtractedContent` để build index.

---
*Active wrapper generated by TNMCORE-OS Product Owner Role.*
