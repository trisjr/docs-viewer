---
id: ST-06
type: story-active
status: in-sprint
sprint: SPRINT-001
sp-original: 5
sp-effective: 3
created: 2026-06-27
---

# 🏃 [Active] ST-06 — Unified Viewer UI

> **Thin status wrapper** cho Sprint 001. **AC & traceability đầy đủ xem story canonical — không lặp lại tại đây:**
> 📄 [`../Backlog/Story-Unified-Viewer-UI.md`](../Backlog/Story-Unified-Viewer-UI.md)
>
> Sprint Plan: [`../../010-Planning/Sprints/Sprint-001.md`](../../010-Planning/Sprints/Sprint-001.md) · SP gốc **5** · SP hiệu dụng **3** (shell + routing đã có; còn wiring state + loading/error thật — xem PO Re-estimate Note §3.2).

## Đóng góp cho Sprint Goal

Là **khung hiển thị thống nhất** của vertical slice: kết dính Upload ↔ View ↔ Extract trong một giao diện, tự nhận diện format và route tới view con. Sprint 1 chỉ exercise nhánh PDF, nhưng khung này là chỗ cắm cho `.docx`/`.xlsx` sau (không cần dựng lại UI).

> Re-estimate 5→3: shell + routing theo `FileFormat` + empty layout đã có trong scaffold. Còn lại là **wiring state thật** + **loading/error states**.

## Task Breakdown (Engineer thực hiện)

- [ ] Wire state thật trong `src/ui/UnifiedViewer.tsx` qua `useAppStore`: tiêu thụ `state.session` / `state.error` để route đúng view con / empty / error.
- [ ] Thêm **loading state** khi `DocumentService.open()` đang chạy (render/extract async qua worker) — tránh khung trống.
- [ ] Hiện thực `src/ui/ErrorBanner.tsx` hiển thị `state.error` thật (render thất bại → trạng thái lỗi thân thiện thay vì crash/khung trống — Unhappy Path 1).
- [ ] Wire "mở tài liệu mới khi đang xem": dispatch `RESET` + `SESSION_OPENED` thay thế nội dung đang xem (UC-02 A2).
- [ ] Đảm bảo điều khiển dùng chung (mở/đóng file, ô search) nhất quán vị trí; điều khiển đặc thù PDF (phân trang/zoom) hiển thị trong khu điều hướng.

## Definition of Done (story-level — map từ AC canonical)

- [ ] Tự nhận diện format → chọn đúng view con + hiển thị trong một UI thống nhất (PDF cho Sprint 1).
- [ ] Điều hướng/điều khiển dùng chung nhất quán vị trí.
- [ ] Mở file mới khi đang xem → thay thế nội dung (UC-02 A2).
- [ ] Render thất bại → trạng thái lỗi thân thiện, không khung trống/crash (Unhappy Path 1).
- [ ] Format không có viewer → thông báo "định dạng không được hỗ trợ" (Unhappy Path 2 / UC-02 E1).

## Dependencies

- **Phụ thuộc:** ST-01 (session để route), ST-03 (PdfView để hiển thị nội dung) — **làm đan xen ST-03**.
- **Được phụ thuộc bởi:** ST-07 (ExtractedContentPanel sống trong khung này).

---
*Active wrapper generated by TNMCORE-OS Product Owner Role.*
