---
id: ST-02
type: story-active
status: in-sprint
sprint: SPRINT-001
sp-original: 8
sp-effective: 2
created: 2026-06-27
---

# 🏃 [Active] ST-02 — Layered Document Processing (enabler) · Verify & Close

> **Thin status wrapper** cho Sprint 001. **AC & traceability đầy đủ xem story canonical — không lặp lại tại đây:**
> 📄 [`../Backlog/Story-Layered-Document-Processing.md`](../Backlog/Story-Layered-Document-Processing.md)
>
> Sprint Plan: [`../../010-Planning/Sprints/Sprint-001.md`](../../010-Planning/Sprints/Sprint-001.md) · SP gốc **8** · SP hiệu dụng **2** (Verify & Close — xem PO Re-estimate Note §3.1).

## Đóng góp cho Sprint Goal

Là **enabler nền** của vertical slice: đảm bảo lát cắt dọc PDF được xây trên kiến trúc tách lớp đúng (Core thuần, extension point cho format & data layer) — để các định dạng sau chỉ cần thêm adapter, không viết lại core. Chạy **cuối sprint** như gate xác nhận extension points vẫn xanh sau khi đã code ST-01/03/06/07.

> Đây là **"Verify & Close"**, KHÔNG phải 8 SP fresh: 3 AC kiến trúc (KR3.1/3.2/3.3) đã thỏa qua walking skeleton. Nhiệm vụ là xác minh + đóng nốt gap.

## Task Breakdown (Engineer thực hiện)

- [ ] Verify **KR3.1** (Core tách rời UI/user): `test/architecture/dependency-rule.test.ts` xanh — `src/core/` không import `ui`/`app`/`data`.
- [ ] Verify **KR3.2** (extension point format): `test/core/AdapterRegistry.test.ts` xanh — `register`/`resolve` đúng, trả `undefined` cho format chưa đăng ký (backstop UC-02 E1).
- [ ] Verify **KR3.3** (extension point auth/multi-tenant): `test/data/InMemoryStorageProvider.test.ts` + `test/composition.test.ts` xanh — `StorageProvider` port + DI tại composition root.
- [ ] Closing assertion (nếu có gap): test khẳng định **đăng ký adapter thứ hai không cần sửa Core** (vd register thêm một adapter giả → resolve được, core không đổi).
- [ ] Xác nhận toàn bộ enabler tests nằm trong build gate `npm test` và **không vi phạm Dependency Rule** sau khi lát cắt PDF hoàn tất.

## Definition of Done (story-level — map từ AC canonical)

- [ ] Logic parse/extract tách rời UI (KR3.1) — verified bằng test.
- [ ] Extension point thêm định dạng mới không sửa core viewer (KR3.2) — verified.
- [ ] Extension point gắn auth/multi-tenant không viết lại core (KR3.3) — verified.
- [ ] Unhappy path: format chưa có processor → lỗi "định dạng không được hỗ trợ" (qua `resolve` trả `undefined`).

> KR3.2 sẽ được **kiểm chứng đầy đủ** ở M4 (khi thêm `.pptx` thật); Sprint 1 chỉ verify cơ chế extension point hoạt động.

## Dependencies

- **Phụ thuộc:** không có (nền tảng đã có trong scaffold) — nhưng **chạy cuối** để verify sau khi các story khác đã code.
- **Được phụ thuộc bởi:** ST-01, ST-03, ST-07 dùng `AdapterRegistry` / `DocumentAdapter` / `StorageProvider` mà story này bảo chứng.

---
*Active wrapper generated by TNMCORE-OS Product Owner Role.*
