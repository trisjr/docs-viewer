---
id: ST-03
type: story-active
status: in-sprint
sprint: SPRINT-001
sp-original: 5
sp-effective: 5
created: 2026-06-27
---

# 🏃 [Active] ST-03 — View PDF

> **Thin status wrapper** cho Sprint 001. **AC & traceability đầy đủ xem story canonical — không lặp lại tại đây:**
> 📄 [`../Backlog/Story-View-PDF.md`](../Backlog/Story-View-PDF.md)
>
> Sprint Plan: [`../../010-Planning/Sprints/Sprint-001.md`](../../010-Planning/Sprints/Sprint-001.md) · SP gốc **5** · SP hiệu dụng **5** (full work — adapter render + worker + view đều stub).

## Đóng góp cho Sprint Goal

Là **lõi "Xem"** của vertical slice: biến PDF đã upload thành nội dung nhìn thấy được trên web (phân trang + zoom). Đây là story chịu **R-01 (độ trung thực render — Score 9, rủi ro số 1)** — chứng minh được PDF render đúng tức là gỡ phần lớn rủi ro kỹ thuật của cả M1.

## Task Breakdown (Engineer thực hiện)

- [ ] Tích hợp **PDF.js** vào `src/core/adapters/PdfAdapter.ts`: hiện thực `render(file)` → `RenderedDocument` (pages canvas).
- [ ] Wire `src/core/worker.ts`: chạy parse/render nặng off-main-thread qua `postMessage` boundary (WorkerRequest `render` cho PDF).
- [ ] Hiện thực `src/ui/views/PdfView.tsx`: render canvas trang, điều khiển **phân trang** (next/prev) + **zoom cơ bản** thay placeholder.
- [ ] Wire `DocumentService.open()` gọi `adapter.render` + `getRendered(sessionId)` trả payload cho `PdfView`.
- [ ] Xử lý unhappy: PDF hỏng → `DocumentSession.status = Failed`, báo lỗi (không render sai); render lỗi nghiêm trọng → thông báo trạng thái (BR-004-2).
- [ ] Đo perf trang đầu trên fixture baseline → đảm bảo ≤ 3s (KR1.3).

## Definition of Done (story-level — map từ AC canonical)

- [ ] Trang đầu PDF hiển thị sau khi render; ≤ 3s trên file baseline (KR1.3).
- [ ] Phân trang next/prev hiển thị đúng trang.
- [ ] Zoom in/out giữ độ trung thực chấp nhận được (KR1.2).
- [ ] PDF hỏng → thông báo lỗi, không render sai (Unhappy Path 1).
- [ ] Render lỗi một phần → báo trạng thái thay vì hiển thị sai lệch âm thầm (Unhappy Path 2).

## Dependencies

- **Phụ thuộc:** ST-01 (session đã `open`), ST-06 (shell Unified Viewer wiring để hiển thị — làm đan xen).
- **Được phụ thuộc bởi:** ST-07 (Extract chạy trên PDF đã mở).

---
*Active wrapper generated by TNMCORE-OS Product Owner Role.*
