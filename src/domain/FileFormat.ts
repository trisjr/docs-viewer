// AI Coding
/**
 * @file FileFormat.ts
 * @description Shared Kernel — enum định dạng lõi MVP (PDF/DOCX/XLSX). Khóa định danh route adapter + tra MAX_FILE_SIZE.
 * Canonical: Spec-Module-Contracts §3.1, DB-Entity §9.1.
 */
export enum FileFormat {
  PDF = 'pdf',
  DOCX = 'docx',
  XLSX = 'xlsx',
}
